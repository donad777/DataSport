<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quản lý sản phẩm</title>
    <link rel="stylesheet" href="/path/to/your/styles.css"> <!-- Link to your CSS file -->
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 20px;
            background-color: #f9f9f9;
        }

        h1 {
            color: #333;
        }

        a.btn {
            display: inline-block;
            padding: 10px 20px;
            margin: 10px 0;
            background-color: #007BFF;
            color: #fff;
            text-decoration: none;
            border-radius: 5px;
        }

        .btn {
            display: inline-block;
            padding: 10px 20px;
            margin: 10px 0;
            background-color: #007BFF;
            color: #fff;
            text-decoration: none;
            border-radius: 5px;
        }

        a.btn.delete {
            background-color: #DC3545;
        }

        p {
            margin: 10px 0;
        }

        .search-input {
            padding: 10px;
            width: 100%;
            max-width: 300px;
            margin-bottom: 20px;
            border: 1px solid #ccc;
            border-radius: 5px;
        }

        .product-table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }

        .product-table th,
        .product-table td {
            padding: 10px;
            border: 1px solid #ddd;
            text-align: left;
        }

        .product-table th {
            background-color: #f4f4f4;
        }

        .product-table tr:nth-child(even) {
            background-color: #f9f9f9;
        }
    </style>
</head>

<body>
    <h1>{{title}}</h1>

    <a href="/add" class="btn">Thêm</a>

    <p>Tìm kiếm</p>
    <form action="/find" method="get">
        <input type="text" class="search-input" placeholder="Nhập tên" name="name" />
        <button type="submit" class="btn">Tìm kiếm</button>
    </form>
    <table class="product-table">
        <thead>
            <tr>
                <th>ID</th>
                <th>Tên sản phẩm</th>
                <th>Giá</th>
                <th>Số lượng</th>
                <th>Loại</th>
                <th>Thao tác</th>
            </tr>
        </thead>
        <tbody>
            {{#each list}}
            <tr>
                <td>{{_id}}</td>
                <td>{{name}}</td>
                <td>{{price}}</td>
                <td>{{quantity}}</td>
                <td>{{category.name}}</td>
                <td>
                    <a href="/edit/{{_id}}" class="btn">Sửa</a> |
                    <a href="/delete/{{_id}}" class="btn delete">Xóa</a>
                </td>
            </tr>
            {{/each}}
        </tbody>
    </table>
</body>

</html>